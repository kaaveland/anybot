To install this library, put the folder somewhere on your python-path OR
from the directory, run the command:
# python setup.py install

When it is on your python-path, you can use it by importing anybot.
For the moment, the only really interesting part of it is the im subpackage.
